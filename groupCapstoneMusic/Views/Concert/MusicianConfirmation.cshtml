@model IEnumerable<groupCapstoneMusic.Models.Concert>

@*@{
    Layout = null;
}*@

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>MusicianConfirmation</title>
</head>
<body>
    @*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Venue)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Venue)
            </td>
            <td>
                @Html.ActionLink("Confirm", "EditConfirm", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id, customer = ViewBag.CID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
